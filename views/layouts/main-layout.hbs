<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="/css/main.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ pageTitle }}</title>
    {{#if formsCSS}}
    <link rel="stylesheet" href="/css/forms.css">
    {{/if}}
    {{#if productCSS}}
    <link rel="stylesheet" href="/css/product.css">
    {{/if}}
</head>

<body>
    <div class="main-header__space"></div>
    <header class="main-header">
        <nav class="main-header__nav">
            <ul class="main-header__item-list">

                <a href="/">
                    <li class="main-header__item">
                        <img class="logo" src="/img/logo.svg">
                    </li>
                </a>
                </li>
                <a class="{{#if homeActive }}active{{/if}}" href="/">
                    <li class="main-header__item">
                        Home
                    </li>
                </a>
                <a class="{{#if activeProducts }}active{{/if}}" href="/shop/products">
                    <li class="main-header__item">
                        Products
                    </li>
                </a>
                {{#if isLoggedIn}}
                <a class="{{#if activeCart }}active{{/if}}" href="/shop/cart">
                    <li class="main-header__item">
                        Cart
                    </li>
                </a>

                <a class="{{#if activeAddProduct }}active{{/if}}" href="/admin/add-product">
                    <li class="main-header__item">
                        Add Product
                    </li>
                </a>
                <a class="{{#if activeProductList }}active{{/if}}" href="/admin/products">
                    <li class="main-header__item">
                        My Products
                    </li>
                </a>
                {{/if}}
            </ul>

            {{#if isLoggedIn}}
            <form action="/admin/logout" method="post">
                <button class="logout-button" type="submit">
                    <li class="main-header__item">
                        Logout
                    </li>
                </button>
            </form>
            {{else}}

            <ul class="main-header__item-list">
                <a class="{{#if activeLogin }}active{{/if}}" href="/admin/login">
                    <li class="main-header__item">
                        Login
                    </li>
                </a>
                <a class="{{#if activeRegister }}active{{/if}}" href="/admin/register">
                    <li class="main-header__item">
                        Register
                    </li>
                </a>
                {{/if}}

            </ul>
        </nav>

    </header>
    {{{ body }}}
    {{!-- <script>
        const loggedInEl = document.querySelector(".loggedin");
        const isLoggedIn = localStorage.getItem("hbs-isLoggedIn");
        loggedInEl.innerHTML = isLoggedIn ? "yep u r logged in" : "oh no ur not!";

        const logoutButton = document.querySelector("#logout");
        logoutButton.addEventListener("click", function () {
            localStorage.removeItem("hbs-isLoggedIn");
        })
    </script> --}}
</body>

</html>